# Sample .travis.yml for R projects

language: r
sudo: false
cache: packages

#bioc_required: true

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change

r_packages:
  - covr

after_success:
  - Rscript -e 'covr::codecov()'
